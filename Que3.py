# A. Id() function returns the unique identifier for the variable.

# B. It depends on the data type of the variable.

#example:
a = 10
b = [10]

#Here the values for a & b are same lets check id() for both:

print(id(a))


print(id(b))
